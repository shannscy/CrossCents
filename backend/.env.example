X8_API_KEY=
X8_SUBACCOUNT_ID=NA_Verif8

# Public Descope project ID (not secret) — used server-side to validate session JWTs.
DESCOPE_PROJECT_ID=P3HZlcn7sECUmwT4OWWOSR72I2fa

# Shared secret with the Descope Generic HTTP Connector (HMAC signing). Must
# match exactly what you put in the connector's "HMAC Secret" field in
# Descope Console. Keep this out of git same as X8_API_KEY.
DESCOPE_CONNECTOR_HMAC_SECRET=
